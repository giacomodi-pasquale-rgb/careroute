{
  "name": "careroute",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:data": "node scripts/validate-data.mjs",
    "build:data": "node scripts/build-web-data.mjs",
    "import:cms": "node scripts/import-cms-hospitals.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate:data && npm test"
  },
  "engines": { "node": ">=20" }
}
