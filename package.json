{
  "name": "careroute",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate:data": "node scripts/validate-data.mjs",
    "build:data": "node scripts/build-web-data.mjs",
    "import:cms": "node scripts/import-cms-hospitals.mjs",
    "reconcile:cms": "node scripts/reconcile-candidates.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate:data && npm test",
    "start:api": "node server/index.mjs"
  },
  "engines": { "node": ">=20" }
}
